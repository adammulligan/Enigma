/**
 * 
 */
package com.adammulligan.uni;

import java.util.Scanner;

/**
 * An interactive console application that generates and stores a pair of RSA keys.
 * 
 * @author adammulligan
 *
 */
public class GenerateKeys {
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		System.out.println("Generating public/private RSA key pair.");
		
		Scanner kb = new Scanner(System.in);
		
		System.out.print("Key length (bits):");
		int length = kb.nextInt();
		
		System.out.print("Output to [~/]:");
		String output_folder = kb.next();
		
		writeFile(generatePrivateKey(),output_folder+"/id_rsa");
		writeFile(generatePublicKey(),output_folder+"/id_rsa.pub");
	}
	
	private int generatePrime(int bits) {
		return 1;
	}
	
	private static String generatePrivateKey() {
		return "1";
	}
	
	private static String generatePublicKey() {
		return "1";
	}
	
	private static void writeFile(String content, String filename) {
	
	}
}
